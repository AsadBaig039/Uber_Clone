import {StyleSheet} from 'react-native';
const styles = StyleSheet.create({
  googleMapStyle: {
    height: '100%',
    width: '100%',
  },
  marker: {
    width: 70,
    height: 70,
    resizeMode: 'contain',
  },
});

export default styles;
